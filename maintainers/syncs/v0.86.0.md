# v0.86.0 synchronization review

This records the adoption decisions for upstream `v0.86.0` (commit `ecac0a9c4`, coding-agent tree `4ed8c7dceea4`) from the `v0.85.1` baseline. Durable rules stay in [Architecture](../architecture.md); per-path intent stays in the ledger.

Reviewed all 186 coding-agent paths changed from `v0.85.1` to `v0.86.0`:

| Paths | Decision |
| --- | --- |
| 79 runtime source paths | Adopted the transcript-backed prompt/tool state (`system` messages, `TranscriptContext`, `getCurrentSystemPrompt()`/`getCurrentTools()`), cost-aware cache warming and `usage` entries, `/bug` reports with crash logs, fail-closed `user_bash`, strict-prefer constrained sampling for built-in tools, lazy jiti/virtual-module loading, the Radius model catalog, `retry.maxAgentDelayMs`, per-model compaction overrides, and the removal of the native clipboard dependency. Retained the registered deviations: percentage compaction trigger and model-window budgets (overrides carry `keepRecentTokens` only), background execution and `BackgroundSession`, editor-submit interception, context snapshots, `resolveModelStreamOptions`, the native tool chrome, and the `@astralyn/pi` package name for extension aliases and virtual modules. |
| Compaction cut point | Retired the distribution's trailing-tool-result cut-point fix in favor of upstream's equivalent (#9740). |
| Compaction cancellation (#9340/#9777) | Adopted upstream's ordering: prepare, emit `compaction_start`, then resolve summarization auth under the cancellable signal. Manual compaction now captures the model, its policy, and the stream before awaiting auth. |
| Context snapshots | Adapted: upstream's forced-prompt projection wraps `transformContext`, so the snapshot capture now installs outermost after `AgentSession` and `systemPrompt` is replayed from the prepared transcript. BTW keeps the transcript's leading `system` message instead of declaring its own. |
| 70 test paths | Adopted 43 verbatim; adapted 25 to this distribution (budgets in `triggerPercent` terms, `keepRecentTokens`-only overrides, `@astralyn/pi` imports and virtual-module keys, the shared tool chrome's progress row, spy-based image-conversion mocking, editor-submit and `clearChat` helpers in interactive mocks, native Windows paths); 2 upstream deletions applied, including `experimental-tool-strict-mode.test.ts` with its flag. Distribution-only tests were updated for transcript `system` messages. |
| 17 documentation paths | Merged into the distribution-owned docs: cache warming, bug reports, transcript system messages, `usage` entries, `TranscriptContext` provider guidance, `keepRecentTokens` model overrides, and `/tree` availability. Kept repository links, standalone development guidance, and navigation. |
| `package.json`, `npm-shrinkwrap.json` | Upgraded the seven `@earendil-works/*` dependencies to `0.86.0` plus chalk, grok-mermaid, ignore, minimatch, semver, typebox, undici, and added `marked` as an exact devDependency; removed the optional `@mariozechner/clipboard`. Kept the distribution's dependency placement and the release-owned package version. |
| `scripts/build-bundle.mjs` | Dropped the clipboard external; allowed `kerberos` as an optional external of the proxy-agent bundle. |
| `CHANGELOG.md`, `README.md` | Recorded the adopted behavior under `[Unreleased]`; the README keeps the distribution's version. |
| 14 example paths | Adopted the three example source updates and removed `kimi-deferred-tools.ts` with its compat field. Not applicable: private example version bumps and lockfiles. |
| 2 `install-lock/` paths | Not applicable: this standalone package owns its shrinkwrap. |

Newly registered deviations: `src/core/extensions/virtual-modules.ts`, `src/experimental/micro/runtime.ts`, and `src/modes/interactive/bug-report.ts`.

Validation on Windows: `npm run check`, a clean build, the baseline gate (`npm run diff:upstream -- --check`), the full isolated suite (337 files, 3191 tests passed; 7 files and 88 tests skipped without API keys), and a clean installation of the packed tarball passed. Full interactive acceptance (cache warming notices, `/bug`, compaction cancellation, BTW after a forced prompt, and `/tree` during compaction) is assigned to the owner.
