# v0.86.1 synchronization review

This records the adoption decisions for upstream `v0.86.1` (commit `13cbf77df`, coding-agent tree `a35108a7fa39`) from the `v0.86.0` baseline. Durable rules stay in [Architecture](../architecture.md); per-path intent stays in the ledger.

Reviewed all 27 coding-agent paths changed from `v0.86.0` to `v0.86.1`:

| Paths | Decision |
| --- | --- |
| 6 runtime source paths | Adopted verbatim: `src/core/model-resolver.ts` (Meta default model), `src/modes/interactive/bug-report.ts` (description prompt through `ExtensionEditorComponent`), `src/modes/interactive/components/extension-editor.ts` (`description` option), `src/utils/clipboard-image.ts` (`isWSL` extraction), `src/utils/clipboard.ts` (WSL/headless OSC 52), and the new `src/utils/wsl.ts`. None of these paths deviates in this distribution, so no ledger entry was added. |
| `src/modes/interactive/interactive-mode.ts` | Merged into the registered `ui` deviation: the pi-ai/compat import became a value import carrying `isRetryableAssistantError`, the new `maybeSuggestBugReport()` gates the hint on non-retryable, non-cancellation errors, the `message_end` error branch and the `auto_retry_end` failure branch now route through it, and the bug-report context receives `keybindings`. Crash reporting keeps the distribution's own instructions text. |
| `src/cli/args.ts` | Merged the `META_API_KEY` line into the registered help-text deviation; the fullscreen-default `--tui-mode` wording and the dropped plan-mode example stay. |
| `scripts/run-source.mjs` | Distribution-local dev launcher: added `META_API_KEY` to `credentialVariables` so `--no-env` strips it. `test/run-source.test.mjs` cross-checks the providers page against that list and failed until the entry was added. |
| 3 test paths | Adopted `test/bug-report.test.ts` and `test/clipboard.test.ts` from upstream and added `test/interactive-mode-bug-report-hint.test.ts`. The only adaptation is the `VirtualTerminal` import in `test/bug-report.test.ts`: upstream's `../../tui/test/virtual-terminal.ts` monorepo path becomes this repository's `./helpers/virtual-terminal.ts`. |
| `package.json`, `npm-shrinkwrap.json` | Upgraded the seven `@earendil-works/*` dependencies to exact `0.86.1` plus the transitive `@earendil-works/pi-telemetry`. Release age exception: `v0.86.1` published on 2026-09-20, before the repository's two-day `min-release-age`, so the two install commands ran with `--min-release-age=0` after review. A stale `node_modules/.package-lock.json` reported "up to date" against the new pins; removing only that cache file and reinstalling resolved it, per [Dependency maintenance](../dependencies.md). The lockfile diff contains only the eight `@earendil-works/*` version/resolution/integrity lines and the root specs. |
| `docs/providers.md` | Ported upstream's Meta content into the distribution-owned page: the `/login` list, the Meta (Muse subscription) section, and the `META_API_KEY` table row. The page's two pre-existing distribution lines (OpenRouter remote-login wording, environment-variables cross-reference) stay. |
| `CHANGELOG.md` | Recorded the adopted behavior under `[Unreleased]`. |
| 9 `examples/` paths | Not applicable: private example version bumps and lockfiles. |
| 2 `install-lock/` paths | Not applicable: this standalone package owns its shrinkwrap. |

Dependency-carried behavior adopted through `@earendil-works/pi-ai` `0.86.1`: the Meta Muse provider with OAuth device flow and `META_API_KEY`, inherited z.ai `Prompt too long` overflow recognition, and Cerebras strict-schema compat. `meta` joins `KnownProvider`, so the model-resolver change and the dependency bump had to land together — `defaultModelPerProvider` is `Record<KnownProvider, string>`. `isRetryableAssistantError` already existed in `0.86.0` through `pi-ai/compat`. Upstream's "Node persistent compile cache" note has no coding-agent source change and no counterpart in this distribution.

No new ledger entries were required and none became stale: every adopted path either matches upstream or stays under an existing entry (`interactive-mode.ts`, `args.ts`, `test/`, `docs/`).

Validation on Windows: `npm run check`, the adopted and interactive-focused tests, the full isolated suite (338 files, 3209 tests passed; 7 files and 88 tests skipped without API keys), the baseline gate (`npm run diff:upstream -- --check`, 511 worktree differences against v0.86.1 with 63 registered deltas), and a clean installation of the packed tarball (`PI_PACKAGE_ALLOW_FRESH=1`, since 0.86.1 is younger than the repository's two-day release age) passed. Full interactive acceptance (`/bug` with a pasted multi-line description, no `/bug` hint on cancellation or a 503, clipboard copy under WSL and in a container) is assigned to the owner.
