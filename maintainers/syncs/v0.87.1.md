# v0.87.1 synchronization review

This records the adoption decisions for upstream `v0.87.1` (commit `f07218c4d4`, coding-agent tree `419732156060`) from the `v0.87.0` baseline. Durable rules stay in [Architecture](../architecture.md); per-path intent stays in the ledger.

Reviewed all 67 coding-agent paths changed from `v0.87.0` to `v0.87.1`:

| Paths | Decision |
| --- | --- |
| 3 runtime source paths clear of deviations | Adopted verbatim: `src/core/compaction/compaction.ts` (rewritten `TURN_PREFIX_SUMMARIZATION_PROMPT` and its `# Conversation`/`# Instructions` framing, from #9908), `src/core/model-resolver.ts` (`xai` default `grok-4.6` → `grok-4.7`), and `src/experimental/services/README.md` (source-only server/client run instructions and `PI_SERVER_DIR`/`PI_SERVER_ID`). |
| `src/cli/args.ts` | Merged the `--mode` validation hunk (#9045) into the registered help-text deviation. A missing value or one starting with `-`, and any value outside `text`/`json`/`rpc`, now push an error diagnostic that `main.ts` prints before `process.exit(1)`; parsing continues so later flags still register. The fullscreen-default `--tui-mode` wording and the dropped plan-mode example stay. |
| 4 test paths | Adopted verbatim: `test/args.test.ts` (the `--mode` describe block), `test/compaction-summary-reasoning.test.ts` and `test/suite/agent-session-compaction.test.ts` (the new conversation-boundary assertions), and `test/model-resolver.test.ts` (Grok 4.7 default). |
| `test/suite/compaction-budget.test.ts` | Distribution-owned test updated for the adopted prompt: the summarization handler now asserts the `# Conversation`/`# Instructions` framing instead of the removed `This is the PREFIX of a turn` text. Without this, the faux provider swallowed the failed assertion as a provider error and the agent retried, turning the `callCount` expectation into a misleading failure. |
| `package.json`, `npm-shrinkwrap.json` | Upgraded the seven `@earendil-works/*` dependencies to exact `0.87.1` plus the transitive `@earendil-works/pi-telemetry`. Release age exception: `v0.87.1` published on 2026-09-22, before the repository's two-day `min-release-age`, so both install commands ran with `--min-release-age=0`. The first resolution also refreshed 18 unrelated AWS SDK/Smithy entries because the age override exposes fresh transitive releases; those entries were restored to their previous resolutions after confirming the new `@earendil-works/*` packages declare no changed AWS/Smithy ranges, leaving a lockfile diff of only the eight `@earendil-works/*` version/resolution/integrity lines and the root specs. A stale `node_modules/.package-lock.json` again reported "up to date" against the new pins; removing only that cache file and reinstalling resolved it, per [Dependency maintenance](../dependencies.md). |
| 5 `docs/` paths | Ported the factual changes into the distribution-owned pages while keeping their prose: `docs/usage.md` documents `--mode text` and the error-plus-nonzero-exit behavior for a missing or invalid `--mode` value, and `docs/compaction.md` drops the stale claim that compaction and branch-summary requests use fresh routing session IDs in favor of upstream's corrected statement that summarization requests disable prompt-cache writes. The remaining 34 changed pages are upstream's documentation restructure (new `cli.md`, `cli-integration.md`, `how-pi-works.md`, `message-types.md`, `rpc-commands.md`, `rpc-extension-ui.md`, removed `development.md`, rewritten navigation) and stay distribution-owned. No page in this distribution enumerates built-in model IDs or the xAI default, so the new models and the Grok 4.7 default needed no page edit. |
| `README.md`, `CHANGELOG.md` | Recorded the adopted behavior under `[Unreleased]`: the new frontier models, the Grok 4.7 default, the split-turn summary fix, the `--mode` validation fix, and the inherited image-only-message and Claude Code version fixes. This distribution's README is a documentation index and gained no new sections. |
| 4 `examples/` paths | Adopted `examples/rpc-extension-ui.ts` verbatim (CRLF-safe stdout framing through `StringDecoder`, and `--no-extension` corrected to `--no-extensions`) and added `examples/rpc-client.ts` with the registered import rename to `@astralyn/pi`. `examples/README.md` gained the CLI-integration section describing the new client, linked to this distribution's [RPC Protocol](../../docs/rpc.md) page instead of upstream's new `cli-integration.md`. `examples/sdk/README.md` needs no change: it already lists `08-prompt-templates.ts`. The 4 package version bumps and 4 lockfiles are private example releases (not applicable). |
| 2 `install-lock/` paths | Not applicable: this standalone package owns its shrinkwrap. |

Dependency-carried behavior adopted through `@earendil-works/pi-ai` `0.87.1`: Claude Opus 5.5 (Anthropic, adaptive thinking, 1M context window), GPT-6 Sol and GPT-6 Luna (OpenAI API keys and Codex subscriptions), and the inherited Copilot variants; image-only user messages no longer include an empty text part (#9797); Anthropic OAuth requests report the current Claude Code version; and Claude Opus 5.5 effort levels are stabilized. Upstream's durable SQLite storage backend and versioned document storage live in the `durable` package, which this distribution does not depend on, so they are not applicable.

Ledger: no entries were added or retired. Every adopted path either matches upstream or stays under an existing entry (`src/cli/args.ts`, `test/`, `docs/`, `examples/`, `package.json`, `npm-shrinkwrap.json`, `README.md`, `CHANGELOG.md`).

Validation on Windows: `npm run check` (including both `tsc` projects with the new example), the focused batch (`test/args.test.ts`, `test/compaction-summary-reasoning.test.ts`, `test/model-resolver.test.ts`, `test/suite/agent-session-compaction.test.ts`, `test/suite/compaction-budget.test.ts`: 183 tests passed), the full isolated suite (342 files and 3290 tests passed; 7 files and 88 tests skipped without API keys), a clean build, and a clean installation of the packed tarball (`PI_PACKAGE_ALLOW_FRESH=1`, since 0.87.1 is younger than the repository's two-day release age) passed. The built `dist/cli.js` and `dist/bundle/cli.js` both report the `--mode` diagnostics and exit with status 1. Interactive acceptance in a real TTY is assigned to the owner: an xAI session defaulting to Grok 4.7, a split-turn compaction producing a single merged summary that the provider accepts, and the new `examples/rpc-client.ts` against a built checkout.
